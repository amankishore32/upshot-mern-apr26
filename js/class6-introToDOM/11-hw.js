/**
 *      Write the function which doubles each value of array and
 *      returns that doubled array as output
 */

const numbers = [4, 45, -5, 65, 85, -1];

function doubleValues(numbers) {
  // TODO
}


/**
 *      Write the function which doubles each value of array and
 *      returns that doubled array as output
 */

const numbers = [4, 45, -5, 65, 85, -1];

function doubleValues(numbers) {
  // TODO
  return numbers.map(number => number * 2);
}

console.log(doubleValues(numbers));